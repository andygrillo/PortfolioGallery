# API Endpoint (update this to your production API endpoint)
VITE_API_ENDPOINT=https://api.andymeira.com

# hCaptcha configuration (get these from hCaptcha dashboard after registering andymeira.com)
VITE_HCAPTCHA_SITE_KEY=your-site-key-here

# Note: Keep your actual .env file private and never commit it to the repository
# The secret key should be used only on the server side, not in this frontend configuration